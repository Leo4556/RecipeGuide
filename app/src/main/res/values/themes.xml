<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Основные параметры для светлой темы -->
        <item name="colorPrimaryDark">@color/main_light_theam2</item>
        <item name="color">@color/background_add_screen</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:color">@color/main_light_theam</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.RecipeGuide" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorButtonNormal">@color/white</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="CustomButton">
        <item name="android:background">#FFDDAF</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.RecipeGuide" parent="Base.Theme.RecipeGuide" />
</resources>