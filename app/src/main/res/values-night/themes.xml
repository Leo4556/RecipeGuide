<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Основные параметры для светлой темы -->
        <item name="colorPrimary">@color/main_dark_theam</item>
        <item name="colorPrimaryDark">@color/main_dark_theam2</item>
        <item name="color">@color/background_add_screen</item>
        <item name="android:windowBackground">@color/background_dark_theam</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.RecipeGuide" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
</resources>